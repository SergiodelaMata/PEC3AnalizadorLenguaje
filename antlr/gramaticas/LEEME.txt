Defensa Ejercicio 01
Después de la línea 21 tendría que terminar con un enddo (fallo en el for)
21: enddo

En las funciones que haya return cambiamos por devolver. Por ejemplo: 68: 
68: return 1; pasa a ser devolver(1);
72: return c1 + c2; pasa a ser devolver(c1 + c2);
77: return (n1>n2); pasa a ser devolver(n1>n2);

Añadimos tipo de dato booleano (cambiamos en el Parser)

Línea 113 eliminarmos el punto y coma (;) es decir:
function imprimirconintro(cadena c):void; pasa a ser function imprimirconintro(cadena c):void


Defensa Ejercicio 02
Línea 62 function imprimirconintro(cadena c):void; quitar el punto y coma function imprimirconintro(cadena c):void

Problam con el if


Defensa Ejercicio 03
Mismo cambios en las funciones que haya return cambiamos por devolver

Problam con el if

Cambiadas las concatenaciones (+ -) para asignarlas antes de usarlas


